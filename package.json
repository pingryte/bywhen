{
  "name": "bywhen",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build:wasm": "wasm-pack build engine --target web --out-dir ../wasm/pkg --release",
    "build:elm": "cd frontend && elm make src/Main.elm --output=dist/elm.js --optimize",
    "build": "npm run build:wasm && vite build",
    "test": "cargo test --manifest-path engine/Cargo.toml && (cd frontend && elm-test) && (cd api && gleam test)",
    "lint": "cargo fmt --manifest-path engine/Cargo.toml -- --check && elm-format frontend/src frontend/tests --validate && (cd api && gleam format --check src test && gleam check)"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "latest",
    "elm": "0.19.1-6",
    "elm-format": "0.8.8",
    "elm-test": "0.19.1-revision16",
    "vite-plugin-elm": "latest",
    "vite": "^7.1.0"
  }
}
